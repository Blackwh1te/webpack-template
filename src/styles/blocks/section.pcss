@import '../mixins.pcss';

.section {
  --paddingY: calc(var(--sectionPaddingY) / 2);

  padding-top: var(--paddingY);
  padding-bottom: var(--paddingY);

  &__header {
    &:not(:last-child) {
      margin-bottom: 48px;
    }
  }
}
